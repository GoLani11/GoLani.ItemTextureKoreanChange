# 저장소 작업 규칙

- 사용자가 지정한 품목만 처리하고 원본 해상도·UV 배치·Diffuse 알파를 보존한다. Normal의 G/A는 투명도가 아닌 방향 채널이다.
- 문자 합성(`masked`) 작업은 비문자 영역을 보존한다. 프로젝트 한글화 스킬의 전체 D 채택(`generated-full`) 작업은 선택한 시안의 비문자 RGB 변화를 허용하되 보존을 검증했다고 주장하지 않는다. 생성 시안의 크기는 같은 종횡비에서만 원본 크기로 보정한다. N/G의 영역·채널 보존 검사는 두 방식에서 동일하다.
- 게임 자산과 추출·생성 텍스처 및 패키지는 workspace/에서 관리하고 Git에 추가하지 않는다. 재사용하는 코드는 localizer/에 둔다.
- 게임 폴더를 원본 입력으로만 사용한다. 테스트 패키지와 게임 확인을 마친 배포본을 구분한다.
- 기본 검사는 python -m pytest 이다. 실제 자산 검사는 로컬 workspace/에만 결과를 남긴다.
