# 저장소 작업 규칙

- SPT 음식·음료 Texture2D의 조사, OCR, 번역, 이미지 생성·수정, Diffuse/Normal/Gloss 처리,
  밉맵·번들 검증 또는 배포 준비 작업에는 `$localize-spt-food-textures` 스킬을 반드시 사용한다.
- 스킬의 차단 게이트를 우회하지 않는다. 필수 검사 도구가 없거나 결과가 불일치·누락된
  경우에는 통과로 간주하지 말고 작업을 중단해 증거와 함께 보고한다.

- 사용자가 지정한 품목을 대상으로 원본 해상도와 UV 배치를 보존한다.
- 보존 대상은 비문자 영역이다. 원문 제거·한글 삽입에 필요한 문자 영역과 그에 연결된 재질 효과는 수정한다. 여기에는 RGB뿐 아니라 알파·Normal·Gloss에 저장된 문자 효과도 포함하며, 알파 전체를 불변으로 취급하지 않는다.
- 수정 범위는 문자와 대응하는 재질 효과의 마스크로 한정하고, 마스크 밖 비문자 영역의 픽셀과 채널은 보존한다.
